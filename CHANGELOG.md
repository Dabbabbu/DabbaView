# 변경 이력

버전 규칙 ([Semantic Versioning](https://semver.org/lang/ko/)):
버그 수정만 → patch (2.1.0 → 2.1.1), 기능 추가·개선 → minor (2.1.x → 2.2.0), 대규모 변경 → major (2.x → 3.0.0).
버전은 `dabbaview/__init__.py`의 `__version__` 하나로 관리합니다 (앱 번들·타이틀 바·About·시작 화면·README).

## 3.8.0 — 2026-09-21

### 추가
- **File ▸ 🧹 캐시** 메뉴 — 기능 찾기(⌘F)에서 `캐시` · `cache` · `임시 파일` · `용량 정리` · `디스크 공간` · `ㅋㅅ`로 바로 찾아 실행합니다.
  - **캐시 지우기…**: 지금 쓰는 양(클라우드 파일 사본 · 폴더 메타데이터 · 썸네일)을 보여 주고 확인 후 모두 지웁니다. 열려 있는 영상과 '저장 위치'에 받아 둔 파일은 그대로 남습니다. 클라우드에서 받는 중에는 끝난 뒤에 하도록 막습니다.
  - **캐시 용량 한도 · 사용량 보기…**: Settings ▸ Cache를 바로 엽니다 (최대 용량 1~50 GB).
- 웹 버전(2.4.0)에도 기능 찾기에 '캐시 지우기' · '캐시 용량 한도' 추가.

## 3.7.1 — 2026-09-21

### 고침
- **클라우드에서 받다가 `IncompleteRead` 오류로 전체가 멈추던 문제**: 네트워크가 잠깐 끊겨 파일 하나가 덜 받아지면(예: 62 KB 받고 끊김) 그 한 파일 때문에 1만 개 받기가 통째로 멈췄습니다.
  - 이제 끊긴 파일은 잠시 뒤(1 · 3 · 6 · 12초) **자동으로 다시 받습니다** (최대 4번). 시간 초과 · 연결 끊김 · 서버 일시 오류(5xx · 429)도 같습니다.
  - 그래도 안 되는 파일만 빼고 나머지는 계속 받습니다. 진행 줄에 `못 받음 N개`, 끝나면 `⚠ N개는 네트워크 문제로 못 받음 — 같은 폴더를 다시 받으면 받은 것은 건너뛰고 나머지만 받습니다`가 나오고, 마우스를 올리면 어떤 파일인지 보입니다.
  - 로그인 만료처럼 다시 해도 안 되는 오류는 예전처럼 바로 멈추고 알립니다.
- **받기 전 '예상 시간'이 늘 기본값(초당 6개 ≈ 1만 개에 28분)이던 문제**: 실제로 받은 속도가 저장되지 않고 있었습니다. 이제 받기가 끝날 때 실제 속도를 기록해 다음 예상부터 반영합니다.

## 3.7.0 — 2026-09-21

### 추가
- **클라우드 창 검색칸의 최근 검색어**: 목록에 안 나오는 폴더(다른 컴퓨터 백업 등)를 찾을 때 매번 이름을 칠 필요가 없습니다.
  - 검색칸을 누르거나(↓ 키) 하면 최근 검색어가 최신순으로 뜨고, 누르면 바로 그 말로 다시 검색합니다.
  - 검색어 옆 **✕**로 하나씩 지우고, 맨 아래 **모두 지우기**로 한꺼번에 지웁니다.
  - 10개까지 기억하며 앱을 다시 켜도 남습니다. 같은 말을 다시 검색하면 맨 위로 올라갑니다.
  - 목록이 떠 있을 때 글자를 치면 목록이 닫히고 검색칸에 그대로 입력됩니다.

## 3.6.4 — 2026-09-21

### 바뀜
- **🔍 기능 검색 칸을 오른쪽 위로**: 셋째 줄 도구 막대 맨 앞의 작은 `찾기` 버튼을, 뷰어 탭 줄(2D View … 3D Volume) **오른쪽 끝**의 검색창 모양 칸 `🔍 기능 검색…  ⌘F`로 바꿨습니다. 파란 테두리로 잘 보이고, 창을 좁혀도 가려지지 않습니다. 마우스를 올리면 "메뉴 · 도구의 모든 기능을 한글 · 영어 · 비슷한 말 · 초성으로 찾아 실행 — 예) 동영상 저장, 내보내기, W/L, ㄷㅇㅅ" 안내가 나옵니다. 열린 창 탭은 이 칸을 덮지 않습니다.

## 3.6.3 — 2026-09-21

### 바뀜
- **Zoom · W/L 조절 칸을 뷰어 창 정가운데 아래로**: 상태바 오른쪽 끝에 있던 두 칸을 영상 영역(2D View … 3D Volume)의 가로 가운데에 놓았습니다. 왼쪽 시리즈 패널을 접거나, 오른쪽 패널(ROI Manager 등)을 열고 닫거나, 창 크기를 바꿔도 곧바로 다시 가운데로 맞춰집니다.

## 3.6.2 — 2026-09-21

### 바뀜
- **맨 아래 상태바 색**: 밝은 파란색(#007acc)이 어두운 화면과 따로 놀던 것을 도구 막대 · 탭과 같은 어두운 회색(#232323, 위쪽에 얇은 구분선)으로 바꿨습니다. 글자는 연한 회색, W/L · Zoom 표시 뒤의 네모 배경은 없앴습니다.

## 3.6.1 — 2026-09-21

### 바뀜
- **Zoom · W/L 조절 칸을 맨 아래 상태바로 옮김**: 영상 오른쪽 세로 막대가 영상 영역 폭을 줄이던 것을 없애고, 창 맨 아래 상태바 오른쪽에 `🔍 Zoom ↕` · `◐ W/L ✥` 두 칸으로 넣었습니다. 영상 크기에 전혀 영향이 없고, 알림 글이 떠도 가려지지 않습니다. 쓰는 법(누른 채 끌기 · 두 번 클릭)은 같습니다.

## 3.6.0 — 2026-09-21

### 추가
- **영상 옆 Zoom · W/L 조절 막대**: 영상 오른쪽에 영상을 가리지 않는 세로 막대를 두었습니다.
  - **🔍 Zoom**: 누른 채 위로 끌면 확대, 아래로 축소 (영상 가운데 기준). 두 번 클릭 = 화면 맞춤.
  - **◐ W/L**: 누른 채 좌우 = Width, 위아래 = Level (영상 위 우클릭 드래그와 같음). 두 번 클릭 = DICOM 기본값.
  - 끄는 동안 칸에 현재 배율 · W/L 값이 보입니다. Multi View에서는 선택한 칸에 적용, MPR · 3D에서는 숨김. **View 메뉴**에서 끄고 켤 수 있습니다(기억됨).

### 바뀜
- **패널 닫기 ✕가 늘 보입니다**: 오른쪽 · 아래 패널(ROI Manager, Image Info, AI Research, Analysis, Histogram, Python Console) 제목 줄의 ✕가 마우스를 올려야만 나오던 것을 항상 보이게 했습니다 (올리면 빨갛게). 떼어내기 버튼은 예전처럼 올렸을 때만.
- **ROI Manager 열고 닫기**: Tools ▸ ROI Manager (Ctrl+Shift+M)를 한 번 더 누르면 닫힙니다. 이미 고른 ROI 도구(ROI · Ellipse · Rect · Area)를 한 번 더 눌러도 열려 있던 ROI Manager가 닫힙니다. Analysis ▸ 패널 보이기/숨기기도 같은 방식으로 부드럽게 닫힙니다.

## 3.5.2 — 2026-09-21

### 고침
- **ROI Manager 패널 버튼이 서로 겹쳐 보이던 문제**: 창 높이보다 패널 내용이 길면 버튼 줄을 억지로 눌러 겹쳤습니다. 이제 버튼 높이는 줄이지 않고 패널에 **스크롤**이 생기며, 표·결과 칸이 먼저 줄어듭니다.
- **옆 탭(열린 창 탭)이 오른쪽 패널 윗부분을 덮던 문제**: 탭은 영상 영역(2D View … 3D Volume 탭 줄) 안에만 놓이고, 자리가 모자라면 글자를 줄입니다.

## 3.5.1 — 2026-09-21

### 고침
- **클라우드에서 받을 때 진행 창이 첫 폴더 숫자로 '완료'라고 하던 문제** (예: 실제로는 10,236개 · 시리즈 207개를 열었는데 `완료 — 1,301개 파일 · 시리즈 27개`): 받는 대로 폴더를 나눠 불러오면서 첫 묶음이 끝나는 순간 창을 '완료'로 닫아 버렸습니다. 이제 여러 폴더를 받을 때는
  - 받는 동안 `지금까지 5,420개 파일 · 시리즈 96개 열림 — 나머지 폴더는 받는 대로 추가` · `받은 폴더 7/14`로 누적해 보여 주고,
  - 모든 폴더를 다 불러온 뒤에야 `완료 — 전체 10,236개 파일 · 시리즈 207개 · 폴더 14개`로 끝납니다 (걸린 시간도 전체 기준).
  - 받는 중에도 **닫기**로 창만 닫을 수 있습니다 (받기·불러오기는 계속).
- **열린 창이 여럿이면 옆 탭에 첫 창만 나오고 나머지는 `+1`로 숨던 문제**: 창마다 탭을 하나씩 3D Volume 탭 오른쪽에 나란히 놓습니다. 누르면 그 창이 앞으로 오고, 자리가 모자라면 탭 글자를 줄입니다.

## 3.5.0 — 2026-09-21

### 추가
- **🔍 기능 찾기 (Ctrl(⌘)+F)**: 메뉴·도구 막대의 모든 기능(233개)을 이름으로 찾아 바로 실행합니다. 도구 막대의 **🔍 찾기** 버튼과 **Help ▸ 🔍 기능 찾기…** 로도 엽니다.
  - 한글·영어·비슷한 말 모두 받습니다: `내보내기` · `export` · `저장` · `동영상` · `동영상으로 저장` · `mp4` → Export as Video…, Batch Export Videos… (여러 말이 함께 맞을수록 위로)
  - 초성 검색(`ㄷㅇㅅ` → 동영상), 메뉴 위치·단축키 표시, ↑↓ · Enter · Esc. 새로 생기는 메뉴도 자동으로 검색됩니다.
- **좌클릭 + 우클릭 함께 누르고 드래그**: **위아래 = 슬라이스**, **좌우 = 위상**(heart cine처럼 위상이 있는 영상). 천천히 끌면 6px마다 한 장, **빠르게 끌수록 많이** 넘어갑니다(최대 12배 — 같은 60px을 천천히 끌면 10장, 확 끌면 46장). 위상 영상의 위아래는 위상을 고정한 채 슬라이스 위치만 옮깁니다. 한쪽 버튼을 먼저 떼도 계속 넘어가고, 먼저 누른 버튼의 동작(W/L · 그리기)은 멈춥니다.

### 바뀜
- **방향키**: **↑ ↓ = 슬라이스**, **← → = 위상** (예전과 반대).
  - 위상이 없는 영상에서는 ↑↓만 쓰고 ← →는 아무 동작도 하지 않습니다.
  - 위상 영상(heart cine 등)의 ↑↓는 위상을 고정한 채 슬라이스 위치를 옮깁니다.
  - **View ▸ ↕ 방향키 ↑↓ 동작** 에서 ↑↓를 잠시 **전체 순서**(슬라이스 1의 위상 1 → … → 마지막 위상 → 슬라이스 2의 위상 1 …) 또는 **위상**으로 바꿀 수 있습니다. Ctrl(⌘)+Shift+↑로 차례로 바꾸고, 앱을 다시 켜면 '슬라이스 위치'로 돌아갑니다.

## 3.4.1 — 2026-09-21

### 고침
- **클라우드에서 받은 폴더 전부에 영상이 없을 때 아무 말 없이 끝나던 문제**: 3.3.1에서 받는 중 폴더마다 경고를 끄면서, 전체가 끝났는데도 영상이 0개인 경우까지 조용히 넘어갔습니다. 이제 **모든 폴더를 다 받고 다 불러온 뒤 한 번** 확인해서, 영상이 하나도 없으면 `받은 폴더 N개에서 볼 수 있는 영상(DICOM)을 찾지 못했습니다`를, 읽기 오류가 있으면 오류 내용과 ⚠ 목록 안내를 보여 줍니다. 받는 중에는 여전히 경고를 띄우지 않고, 로컬 폴더는 예전과 같이 바로 알립니다.

## 3.4.0 — 2026-09-21

### 추가
- **'끝나면 자동으로 닫기' 선택**: 불러오기 창과 클라우드 창 아래에 체크 상자를 넣었습니다.
  - **체크**: 다 끝나면 `완료`를 잠깐(1.2초) 보여 주고 자동으로 닫힙니다.
  - **해제**: 창이 남아 결과를 보여 줍니다 — `완료 — 10,236개 파일 · 시리즈 207개 · 걸린 시간 17초` / `완료 — 파일 10,243개 · 5.5 GB 받음 · 폴더 14개`. 취소·강제 중단 버튼은 사라지고 **닫기** 버튼이 켜집니다(진행 중에는 비활성). ESC로도 닫힙니다.
  - 설정은 기억되고 두 창이 같은 값을 씁니다.
- 클라우드 창을 남겨 두어도 **영상 불러오기는 다 받는 즉시 시작**합니다 (예전에는 창을 닫아야 불러왔음).

## 3.3.1 — 2026-09-21

### 고침
- **클라우드에서 받는 중에 'No DICOM files found' 경고가 뜨던 문제**: 증례 폴더마다 맨 위에 텍스트 파일이 하나씩 있어, 폴더 단위로 받을 때 **텍스트 1개짜리 폴더가 가장 먼저 '다 받은 폴더'가 되고** 그걸 영상으로 열려다 경고가 났습니다(파일 유형 필터는 정상이었음). 이제
  - 볼 영상이 없는 묶음은 열지 않고 다음 영상 묶음에 붙여 넘기며,
  - 받는 중 도착한 묶음에서 영상이 안 나와도 경고창 대신 상태바에 `아직 다운로드 중입니다. 받은 시리즈부터 표시합니다.`만 보여 줍니다.
  - 실제 구글 드라이브 `heart/CT` 데이터(증례 3개 · 3,630개)로 확인: 경고 0회, 시리즈가 받는 대로 2.3초 24개 → 4.8초 76개.
- **텍스트 파일이 영상으로 읽히려다 ⚠ 목록에 오류로 남던 문제** — 파일을 직접 넘겨도 텍스트는 영상으로 읽지 않습니다.
- **같은 폴더 텍스트가 다른 증례 검사에 붙을 수 있던 문제**: 짝을 찾을 때 상위 폴더로 거슬러 올라가, 여러 증례가 한 폴더 아래 있으면 엉뚱한 검사에 붙을 수 있었습니다. 이제 **텍스트가 있는 폴더 안쪽의 검사 하나**일 때만 연결하고, 검사가 여럿이면 연결하지 않으며, 짝이 될 영상이 나중에 도착하면 그때 연결합니다.

## 3.3.0 — 2026-09-21

### 변경 — '빠른 열기(볼 때 받기)'를 없애고 '다 받은 시리즈부터 보기'로
- **빠른 열기를 제거했습니다.** 시뮬레이션(구글 한도 초당 약 27건 · 요청 지연 0.3~0.8초)으로 따져 보니
  - 천천히 볼 때는 괜찮지만(멈칫 1~5%), **휠을 빠르게 굴리면 100% 멈칫 · 최대 15초 대기**, **Multi View 4칸은 최대 60초 대기**
  - 헤더 + 나중에 전체로 **요청이 2배**라 한도 안에서는 오히려 손해
  - 영상을 어떤 속도·순서로 넘길지 예측할 수 없어 판독용으로 쓸 수 없다고 판단했습니다.
- **대신: 폴더(≈시리즈) 단위로 차례대로 받고, 다 받은 시리즈부터 바로 목록에 나타납니다.** 나머지는 뒤에서 계속 받고, 상태바에 `☁ 준비된 폴더 12/44 · 나머지 받는 중`이 표시됩니다. 다 받은 시리즈는 **멈칫 없이** 볼 수 있고, 새 시리즈가 도착해도 지금 보고 있는 화면은 바뀌지 않습니다.
- 동시에 받는 파일 수 6 → 10개 (한도 안쪽, 넘치면 자동 재시도).
- 열기 방식 선택(빠른 열기/전체 다운로드), File ▸ 클라우드 영상 전체 받기, 상태바 '☁ N장 미수신', MPR·3D 시작 전 안내는 모두 필요 없어져 없앴습니다.

### 고침
- **불러오기가 끝나도 진행 창이 닫히지 않던 문제 (근본 원인)**: ESC를 '취소'로 쓰려고 바꿔 둔 `reject()`를 Qt의 창 닫기가 내부에서 불러, **프로그램이 창을 닫으라고 해도 매번 무시**되고 있었습니다(2.15.0부터). 창은 이제 확실히 닫히고, ESC는 여전히 '취소'로 동작합니다. 오늘 '버튼이 안 먹힌다', '경과 4시간'으로 보이던 증상이 모두 이것이었습니다.

## 3.2.9 — 2026-09-21

### 고침
- **DabbaView를 닫아도 클라우드 창이 남아 앱이 계속 떠 있던 문제**: 클라우드 창을 독립 창으로 바꾸면서 메인 창과 함께 닫히지 않았고, 창이 하나라도 남아 있으면 앱이 끝나지 않았습니다(Dock에 남고 CPU 0%로 대기). 이제 메인 창을 닫으면
  - 클라우드 창·설정 창 등 **모든 팝업을 함께 닫고**,
  - 돌고 있던 다운로드·폴더 훑기·미리 받기를 **멈춘 뒤**,
  - 앱을 **확실히 종료**합니다.

## 3.2.8 — 2026-09-21

### 고침
- **상태줄 오른쪽이 잘리던 문제**: 고정폭 글꼴로 바꾸면서 줄이 창보다 길어졌습니다. 문구를 줄였습니다 — `빠른 열기 ·    249 / 10,243 (  2.4%) · 남음  7분 21초 · 경과     11초 · 헤더 4.9 MB`.

### 변경
- 헤더 동시 요청 12 → **18개**: 이 프로젝트의 실제 한도(1인당 분당 325,000단위, 다운로드 1건 ≈ 200단위 → 초당 약 27건) 아래에서 최대한. 넘치면 자동 감속이 동작합니다.

## 3.2.7 — 2026-09-21

### 고침
- **예상 시간이 터무니없이 짧게 나오던 문제** (예: 8,732개 헤더를 `예상 34초`): 용량 ÷ 속도로 계산하고 있었는데, 헤더 받기는 **요청 횟수**가 시간을 좌우합니다. 이제
  - 빠른 열기: **파일 개수 ÷ 초당 개수**
  - 전체 다운로드: 용량 ÷ 대역폭과 개수 ÷ 초당 개수 중 **더 오래 걸리는 쪽**
  - 처음엔 어림값(헤더 초당 15개, 전체 8 MB/s·초당 6개)으로 `예상 약 9분 42초`처럼 표시하고, **한 번 받아 보면 실제 잰 속도를 저장**해 다음부터는 '약' 없이 실측 기반으로 보여 줍니다.

## 3.2.6 — 2026-09-21

### 고침
- **구글 드라이브 요청 한도(403 rateLimitExceeded)로 열기가 멈추던 문제**: 3.2.5에서 동시 요청을 40개로 올린 탓에 분당 허용량을 넘었습니다.
  - 동시 요청을 **40 → 12개**로 되돌리고,
  - 한도에 걸리면 **스스로 속도를 낮췄다가(최대 20초) 회복**하도록 했습니다 (성공하면 벌점을 절반씩 줄임),
  - 헤더 요청은 0.5→1→2→4→8→16초로 **최대 6번 재시도**, 전체 파일 받기도 자동 재시도(num_retries=5),
  - **한 파일이 끝내 실패해도 전체 작업은 계속**되고 실패 개수만 따로 셉니다.
  - 진행 줄에 `구글 요청 한도로 잠시 천천히 (3회)`가 표시되고, 오류 창도 원문 대신 알아보기 쉬운 설명으로 바뀝니다.

## 3.2.5 — 2026-09-21

### 변경
- **빠른 열기 속도 추가 개선**: 헤더 동시 요청 **24 → 40개**, 받는 앞부분 **64 KB → 32 KB**(512×512 CT 기준 메타데이터가 32 KB 안에 모두 들어감을 확인). 구글 쪽 요청 제한(403·429)이나 일시적 서버 오류가 나면 **0.4초씩 늘려 가며 최대 4번 재시도**합니다.
- 10,243개 기준: 20분대(3.2.1) → 5분 51초(3.2.2) → **3분 안팎** 예상.

### 고침
- **진행 줄이 좌우로 흔들려 눈이 피로하던 문제**: 숫자 길이가 바뀔 때마다 뒤 글자가 밀렸습니다. 이제 **자릿수를 고정**하고(`  3,247 / 10,243 파일 ( 31.7%)`) 숫자는 고정폭 글꼴로 그려서, 값만 바뀌고 글자는 제자리에 있습니다. 불러오기 창도 같습니다.

## 3.2.4 — 2026-09-21

### 고침
- **불러오기가 끝났는데 창이 남아 버튼이 하나도 안 먹히던 문제**: 작업이 이미 끝난 상태에서는 '강제 중단'이 "중단할 작업이 없다"며 아무 일도 하지 않았습니다. 이제 **창을 닫아 주고**, 1초마다 도는 감시 타이머가 **남은 창을 스스로 정리**합니다.
- **경과 시간이 0초에 멈춰 있던 문제**: 진행 창이 마지막 문구를 그대로 다시 쓰고 있었습니다. 이제 **지금 기준으로 다시 계산**합니다.
- **'취소' 뒤에도 '강제 중단'을 누를 수 있게** 하고, 정리가 3초 넘게 걸리면 `정리가 오래 걸리면 '강제 중단'을 누르세요` 안내를 덧붙입니다.

## 3.2.3 — 2026-09-21

### 추가
- **확장자 목록 맨 밑에 훑기 진행률**: 유형을 고르는 동안 목록 바로 아래에 `⏳ 확인 중… 68% · 폴더 39/57 · 파일 8,732개 · 남은 시간 약 12초` 줄이 나옵니다. 다 훑으면 사라집니다 — 언제까지 기다려야 하는지 목록에서 바로 알 수 있습니다.

## 3.2.2 — 2026-09-21

### 변경
- **빠른 열기(헤더 받기) 속도 개선**: 헤더는 64 KB뿐이라 전송보다 **왕복 대기**가 대부분입니다. 그래서
  - 헤더 전용 동시 요청을 **6개 → 24개**로 (전체 파일 받기는 대역폭이 병목이라 6개 유지)
  - **연결 재사용**(스레드별 keep-alive 세션)으로 파일마다 TLS 악수를 다시 하지 않게
  - certifi 인증서를 **한 번만 읽도록 캐시**
  - 같은 조건 모의 시험에서 **약 3.9배** 빨라졌습니다 (39개/초 → 151개/초). 10,243개 기준 20분대 → 5분 안팎으로 줄어듭니다.

## 3.2.1 — 2026-09-21

### 고침
- **'유형 골라서 받기'에서 체크가 저절로 풀리던 문제**: 하위 폴더를 훑는 동안 새로 발견된 확장자가 체크에서 빠져 `고른 유형: 0개`가 되곤 했습니다. 이제 **끈 유형만 기억**하고 새로 나온 유형은 계속 켜진 상태로 둡니다.
- **확장자 표가 1~2줄만 보이던 문제**: 표를 펼치면 **6줄 이상** 보이도록 폴더 목록 높이를 자동으로 조정합니다 (폴더 목록도 최소 5줄 유지). 접으면 원래대로 폴더 목록이 넓어집니다.

## 3.2.0 — 2026-09-21

### 추가
- **화면 크기(UI 배율) 조절**: 도구 막대·탭·메뉴 글자가 작아 보일 때 **View ▸ 🔍 화면 크기**에서 키울 수 있습니다. **크게 `⌘⇧+` / 작게 `⌘⇧-`** 또는 90 · 100 · 110 · 125 · 150 · 175% 중에서 직접 고르면 되고, 고른 값은 다음에 열 때도 유지됩니다.

## 3.1.2 — 2026-09-21

### 변경
- **폴더 목록을 다시 넓게**: 창 아래 긴 설명 문구(사용법·캐시 경로)를 없애고 목록에 공간을 돌려줬습니다. 폴더가 1~2줄만 보여 고르기 어렵던 문제 해결 — 이제 기본 18줄 정도 보입니다. 사용법 안내는 목록 위에 마우스를 올리면 나오는 툴팁으로 옮겼습니다.
- 창 기본 크기를 980 × 720으로, 확장자 표 높이는 조금 줄였습니다.

## 3.1.1 — 2026-09-21

### 변경
- **받을 파일을 선택지로**: `받을 파일: (•) 전부 받기  ( ) 유형 골라서 받기`. 골라서 받기를 고르면 확장자 표가 **많은 순서**로 펼쳐지고(하위 폴더까지 자동으로 훑음) 모두 체크된 상태에서 필요 없는 유형만 끄면 됩니다. 'DICOM만' 버튼은 없앴습니다.
- 기본값을 **전부 받기**로 — 폴더 맨 위에서 `0개 · 0 B`로 보이던 문제도 함께 해결.

### 고침
- 독립 창으로 바꾼 클라우드 창이 **밝은 테마로 보이던 문제** — 메인 창의 어두운 테마를 그대로 적용.

## 3.1.0 — 2026-09-21

### 추가
- **받을 파일 유형 고르기**: 한 검사가 DICOM·JPG·PNG로 중복 저장된 경우 필요한 것만 받습니다. ▸ 자세히 표에서 **확장자마다 체크**하고, **DICOM만 / 전부** 버튼으로 한 번에 고를 수 있습니다. 고르는 즉시 `받을 유형: 10,243개 / 전체 30,722개 · 5.5 GB · .dcm` 와 방식별 **예상 시간**(`⚡ 빠른 열기 … 약 640 MB · 예상 1분 20초` / `⬇ 전체 다운로드 … 5.5 GB · 예상 11분 43초`)이 갱신됩니다. 기본값은 **DICOM만**이고, 예상 시간은 직전 다운로드에서 관찰한 속도로 계산합니다.
- **열린 창 탭을 3D Volume 탭 옆으로**: 탭 줄과 같은 높이·모양으로 붙어 한눈에 들어옵니다.
- **모든 팝업이 자동으로 탭에 등록**: 기능마다 따로 손대지 않아도 새로 열리는 창(설정 ⚙ · 진행 ⏳ · 클라우드 ☁ 등)이 자동으로 탭에 나타납니다.

### 변경
- **'이 폴더 전체 열기'** 버튼에 폴더 이름 표시 (`📂 'heart' 폴더 전체 열기`) — 어디를 여는지 분명하게. 툴팁에 포함되는 하위 폴더(CT, MR)도 표시.
- 클라우드 탐색에서 그림 파일을 자동으로 빼지 않고 **유형 선택으로 사용자가 정하도록** 바꿨습니다 (로컬 폴더 열기의 자동 제외 규칙은 그대로).

## 3.0.4 — 2026-09-21

### 변경
- **'이 폴더 전체 열기'가 어느 폴더인지 분명하게**: 버튼에 폴더 이름이 들어갑니다 — `📂 'heart' 폴더 전체 열기`. 툴팁에는 포함되는 하위 폴더(`CT, MR`)까지 보여 줍니다. **선택 항목 열기**는 툴팁에 고른 이름을 표시합니다.

## 3.0.3 — 2026-09-21

### 고침
- **진행 중 글자·창 크기가 계속 흔들리던 문제**: 글자 길이에 따라 창이 늘었다 줄었다 했습니다. 이제 창 너비가 고정되고, 회전 표시(◐◓◑◒)는 폭이 고정된 별도 칸에 있어 **본문 글자가 밀리지 않습니다**.
- **클라우드 창이 메인 창 뒤로 숨던 문제**: 독립 창으로 바뀌어 창 전환(⌘\`)·미션 컨트롤에 따로 나오고, 다른 프로그램을 쓰다 돌아와도 찾기 쉽습니다.
- **메인 창 옆에 '열린 창' 탭**: 클라우드 탐색·불러오기 창이 떠 있으면 메인 창 오른쪽에 `☁ Open from Google Drive — 1.3% (154/11,436)` 탭이 붙습니다. **누르면 그 창이 바로 앞으로** 나오고, 창이 닫히면 탭도 사라집니다. 창이 여러 개면 `+1`처럼 개수가 표시됩니다.
- **창 제목에 진행률 표시**: `Open from Google Drive — 1.3% (154/11,436)` — 창이 가려져 있어도 제목만 보고 진행 상황을 알 수 있습니다.

## 3.0.2 — 2026-09-21

### 고침
- **빠른 열기에서 SSL 오류(`CERTIFICATE_VERIFY_FAILED`)로 열리지 않던 문제**: 번들 앱은 기본 인증서 저장소가 비어 있어 certifi를 써야 하는데, 새로 만든 부분 다운로드 코드에서 빠져 있었습니다. 혹시 부분 다운로드가 실패해도 **자동으로 전체 받기로 넘어가** 열리도록 했습니다.
- **진행 정보 형식이 바뀌며 `too many values to unpack` 오류가 나던 문제** (상태바 ⚠ 내부 오류).
- **'멈춘 것처럼 보이는' 문제 전반**: 오래 걸리는 작업에 **움직이는 표시(◐◓◑◒)** 와 **계속 늘어나는 경과 시간**을 넣어, 새 소식이 없어도 진행 중임을 알 수 있게 했습니다. 3초 넘게 응답이 없으면 `(마지막 응답 12초 전 — 큰 파일이면 시간이 걸립니다)`를 덧붙입니다.
- **진행률이 띄엄띄엄 올라가 멈춘 것처럼 보이던 문제**: 0.5초마다 스스로 갱신하고, 퍼센트를 소수점까지 보여 줍니다 (`86 / 11,436 파일 (0.8%) · 경과 12초 · 남은 시간 약 2분 19초`). 파일 하나가 끝나길 기다리는 동안에도 경과·속도·남은 시간이 계속 움직입니다.

## 3.0.1 — 2026-09-21

### 고침
- 폴더만 들어 있는 곳에서 **▸ 자세히 버튼이 보이지 않던 문제**: 파일이 하나도 없으면 버튼을 숨기고 있었는데, 정작 그때 하위 폴더 내용을 확인해야 해서 필요했습니다. 이제 하위 폴더가 있으면 항상 보입니다.

## 3.0.0 — 2026-09-21

클라우드에서 바로 읽고 보는 방식이 자리를 잡아 **3.0**으로 올립니다.
2.14 ~ 2.19.1에서 쌓은 변화를 한 묶음으로 정리하면:

### 클라우드 (Google Drive · OneDrive)
- **⚡ 빠른 열기**: DICOM 헤더(64 KB)만 먼저 받아 시리즈 목록을 즉시 띄우고, 여는 영상만 그때 내려받습니다. 안 보는 시리즈는 받지 않습니다 — API로 연결했을 때만 가능한 방식입니다.
- 시리즈를 열면 나머지를 **백그라운드로 미리 받아** 스크롤이 끊기지 않습니다.
- **오프라인 대비**: File ▸ ☁ 클라우드 영상 전체 받기(⌘⇧D), 상태바 `☁ N장 미수신`, MPR·3D·일괄 동영상 시작 시 안내.
- **전체 검색**: 구글 드라이브 '다른 컴퓨터'(백업된 PC) 폴더처럼 목록에 없는 곳도 이름으로 찾습니다.
- **열기 전 내용 확인**: 하위 폴더까지 훑어 확장자별 개수·용량을 빈도순으로 보여 줍니다.
- **저장 폴더 지정**(기본: OS 기본 다운로드 폴더/DabbaView) + 남은 디스크 공간 표시.

### 불러오기 · 진행 표시
- 진행률·속도·**남은 시간**을 모든 긴 작업에서 같은 형식으로: `130 / 400 장 (32%) · 경과 1분 5초 · 2.0장/초 · 남은 시간 약 2분 15초`
- **강제 중단** 버튼 — 응답 없는 파일을 기다리지 않고 즉시 창을 닫습니다.
- DICOM과 그림이 섞인 폴더에서 그림 수만 장을 읽다 멈추던 문제 해결.

### 창·설정
- 클라우드 탐색 창·진행 창이 **모달이 아닙니다** — 띄워 둔 채 뷰어를 씁니다.
- 팝업이 뒤로 숨어 앱이 멈춘 것처럼 보이던 macOS 문제 해결.
- 키체인 암호를 반복해서 묻지 않습니다(기본: 앱 설정 파일, Settings ▸ Cloud에서 변경).
- **Settings를 File 메뉴에 고정**.

## 2.19.1 — 2026-09-21

### 변경
- **'열기' 전에 하위 폴더 내용 확인**: 폴더만 들어 있는 곳에서 `파일 0개 · 용량 0 B`로 보이던 문제를 고쳤습니다. **▸ 자세히**를 누르면 하위 폴더까지 훑어 확장자별 개수·용량을 **빈도순**으로 채우고, 훑는 동안 `하위 폴더 확인 중… 폴더 12/34 · 파일 5,120개 · 265 MB · 경과 8초`처럼 진행이 보입니다.
- 창 아래 줄에 중복해서 나오던 `폴더 N개 · 파일 N개`를 없애고, 그 자리에 **하위까지 합친 결과**를 보여 줍니다.
- 열기 방식 아래의 설명 문구를 없애고, **예상 용량을 버튼 이름에** 넣었습니다 — `⚡ 빠른 열기 (권장 · 약 714.2 MB)` / `⬇ 전체 다운로드 (오프라인 대비 · 약 5.9 GB)`.
- 두 열기 버튼의 차이를 분명히 했습니다: **📂 이 폴더 전체 열기**(지금 들어와 있는 폴더 통째로) / **선택 항목 열기 (N개)**(목록에서 고른 것만, 안 고르면 비활성).

## 2.19.0 — 2026-09-21

### 추가
- **지금 보고 있는 폴더의 확장자 목록**: '열기'를 누르기 전에도 `이 폴더: 폴더 3개 · 파일 4개 · 용량 1.1 MB`와 **▸ 자세히**의 확장자별 표(**빈도순**: `.dcm 2개`, `.txt 1개`, `.jpg 1개`)를 볼 수 있습니다.

### 변경
- **열기 방식을 창에서 바로 고릅니다**: 체크박스 대신 **⚡ 빠른 열기 (권장)** / **⬇ 전체 다운로드 (오프라인 대비)** 두 가지 중 선택하고, 고른 방식에 따라 **예상 용량**이 바로 표시됩니다 (예: 빠른 열기 약 714 MB ↔ 전체 다운로드 약 5.9 GB).

## 2.18.0 — 2026-09-21

### 추가
- **⚡ 빠른 열기 (클라우드 부분 다운로드, 권장 · 기본 켜짐)**: 인터넷이 되는 환경이라면 항상 켜 두는 쪽이 낫습니다. 구글 드라이브·OneDrive를 **API로 연결했을 때만** 가능한 기능입니다. 파일 앞부분 64 KB(DICOM 메타데이터)만 Range 요청으로 받아 **시리즈 목록을 바로** 띄우고, 실제로 영상을 열 때 그 파일만 통째로 내려받습니다.
  - 예: 11,428개 · 6.3 GB → 헤더 약 700 MB만 먼저 받고, 보는 시리즈만 추가로 받음
  - 안 보는 시리즈는 **한 번도 받지 않습니다**
  - 받는 동안 상태바에 `클라우드에서 영상을 받는 중… IM0001.dcm`, 끝나면 남은 헤더 파일 수를 표시
  - 이미 캐시에 전체 파일이 있으면 그대로 씁니다 (다시 받지 않음)
  - 동기화 폴더(구글 드라이브 데스크톱 앱)로 열면 한 바이트만 읽어도 OS가 파일 전체를 받으므로 이 방식이 원천적으로 불가능합니다 — **API 연동의 실질적인 이점**입니다
  - 탐색 창의 체크박스로 끌 수 있습니다 (끄면 예전처럼 전부 받은 뒤 엽니다)
- **버퍼링 방지 — 미리 받기**: 시리즈를 열면 그 시리즈의 나머지 영상을 백그라운드로 6개씩 미리 받습니다. 첫 장만 잠깐(보통 0.1~0.3초) 기다리고 이후 스크롤은 끊기지 않습니다.
- **선택한 폴더 요약**: 폴더를 열기 전에 `선택한 폴더: 폴더 3개 · 파일 103개 · 용량 48.5 MB · 그림 400장 제외(DICOM과 섞임)`를 보여 주고, **▸ 자세히**를 누르면 확장자별 개수·용량 표가 펼쳐집니다 (다시 누르면 접힙니다).
- **저장 위치에 남은 디스크 공간 표시**: `저장 위치: …/Downloads/DabbaView · 남은 공간 89.9 GB / 460.4 GB`. 5 GB 미만이면 주황색으로 바뀌고, 받을 용량이 남은 공간보다 크면 **받기 전에 알려 줍니다**.
- **오프라인 대비**:
  - **File ▸ ☁ 클라우드 영상 전체 받기…** — 남은 장수와 용량을 알려주고, 진행률·남은 시간을 보며 한 번에 받습니다 (중간 취소 가능)
  - 상태바에 **☁ N장 미수신** 표시 — 클릭하면 바로 전체 받기
  - **MPR · 3D · 일괄 동영상 내보내기**처럼 영상 전체가 필요한 기능을 시작할 때 아직 받지 않은 영상이 있으면 경고하고 *지금 모두 받기 / 그냥 진행 / 취소*를 고르게 합니다

## 2.17.0 — 2026-09-21

### 추가
- **오래 걸리는 작업에 진행률·남은 시간 표시**: `130 / 400 장 (32%) · 경과 1분 5초 · 2.0장/초 · 남은 시간 약 2분 15초` 형식으로 통일했습니다 (`progress_text.Eta`).
  - 클라우드 **폴더 확인**: `폴더 12/34 · 파일 16,866개 찾음 · 경과 …` (하위 폴더를 발견하며 전체 수가 늘어납니다)
  - 클라우드 **다운로드**: `67/11,428 파일 (0%) · 33.7 MB / 6.3 GB · 4.2 MB/s · 남은 시간 약 …`
  - **일괄 동영상 내보내기 · 익명화 · DICOM Send**에도 같은 표시
- **클라우드 다운로드 폴더 지정**: 받은 파일이 어디에 저장되는지 탐색 창에 항상 보이고(`저장 위치: …`), **폴더 열기 / 변경…** 버튼과 Settings ▸ Cloud에서 바꿀 수 있습니다. 기본값은 운영체제 기본 다운로드 폴더 아래 `DabbaView`(macOS `~/Downloads/DabbaView`, Windows `다운로드\\DabbaView`)이고, 그 아래 `gdrive_20260921_0044` 같은 날짜 폴더로 저장됩니다.

## 2.16.1 — 2026-09-21

### 고침
- **macOS 키체인 암호를 계속 묻던 문제**: 앱을 새로 빌드할 때마다 서명이 바뀌어 macOS가 "키체인 접근을 허용하시겠습니까?"를 반복해서 물었고, 거부해도 설정 파일로 넘어가 그냥 열려 물음이 무의미했습니다. 이제 **로그인 토큰·Client Secret을 앱 설정 파일에 저장하는 것이 기본**이라 암호를 묻지 않습니다. 키체인에 두고 싶으면 Settings ▸ Cloud의 체크박스를 켜세요.
- **팝업이 포커스를 도로 빼앗던 문제**: 2.15.1에서 넣은 '팝업 앞으로 올리기'가 앱이 뒤로 갈 때도 동작해서, 다른 앱(특히 macOS 암호 입력 창)으로 전환하면 포커스를 되가져왔습니다. 이제 **DabbaView가 앞에 있을 때만** 올립니다.

### 변경
- **클라우드 탐색 창이 모달이 아닙니다**: 구글 드라이브/OneDrive 탐색·다운로드 중에도 뷰어를 그대로 쓸 수 있습니다.
- **Settings를 File 메뉴에 고정**: macOS가 자동으로 앱 메뉴(Preferences)로 옮겨 찾기 어려웠습니다. 이제 **File ▸ ⚙ Settings… (환경설정)**에 그대로 있고, 앱 메뉴의 Preferences도 함께 동작합니다.

## 2.16.0 — 2026-09-21

### 추가
- **클라우드 탐색기에 전체 검색**: 위쪽 🔍 칸에 폴더 이름을 넣고 Enter를 누르면 드라이브 전체에서 찾아 바로 열 수 있습니다. 구글 드라이브 **'다른 컴퓨터'(백업된 PC)** 폴더는 내 드라이브 목록에 나오지 않아 접근할 수 없었는데, 이제 이름으로 찾으면 됩니다. OneDrive도 같은 방식으로 검색합니다.

## 2.15.1 — 2026-09-21

### 고침
- **설정 창·경고창을 열어 둔 채 다른 앱에 갔다 돌아오면 아무것도 눌리지 않던 문제**: macOS에서 앱을 다시 활성화하면 메인 창이 앞으로 오는데, 팝업이 뒤에 가려진 채 메인 창은 입력을 받지 못해 앱 전체가 먹통처럼 보였습니다. 이제 앱으로 돌아올 때 떠 있는 팝업을 자동으로 앞으로 올립니다.

## 2.15.0 — 2026-09-21

### 추가
- **불러오기 진행 창을 새로 만들었습니다**: 단계 이름 · `1,234 / 10,236 파일 (12%)` · 경과 시간 · 속도(개/초) · **남은 시간 예상**이 함께 보입니다. 클라우드 폴더처럼 오래 걸릴 때 지금 무엇을 하는 중인지 알 수 있습니다.
- **강제 중단 버튼**: 5초 넘게 걸리거나 진행이 멈추면 나타납니다. **취소**는 지금까지 읽은 영상을 열고(응답 없는 파일을 최대 40초까지 기다릴 수 있음), **강제 중단**은 기다리지 않고 즉시 창을 닫습니다. 남은 읽기 스레드는 뒤에서 스스로 정리되고 화면은 바로 돌아옵니다.
- 진행이 멈추면 어떤 파일에서 몇 초째 막혔는지 진행 창에 주황색으로 표시합니다.

## 2.14.0 — 2026-09-20

### 고침
- **큰 폴더를 열 때 "파일 목록 확인 중"에서 멈춘 것처럼 보이던 문제**: 이미지(JPG·PNG)를 읽는 동안 진행률·단계 이름이 갱신되지 않고 취소도 듣지 않았습니다. 이제 이미지 한 장마다 진행률이 올라가고, 단계 이름이 "이미지 읽는 중 (폴더명)"으로 바뀌며, **취소를 누르면 그 자리에서 멈춥니다**. 오래 걸리는 파일은 멈춤 경고에도 이름이 나옵니다.
- **클라우드 경고를 이미지·NIfTI까지 포함해 읽기 전에 표시**: 지금까지는 DICOM만 세어 물어봤고, 그림 파일은 질문 없이 먼저 내려받았습니다. 이제 받지 않은 파일 전체(개수·용량)를 읽기 전에 물어보고, '건너뛰기'를 고르면 받지 않은 그림도 건너뜁니다.

### 변경
- **DICOM과 그림이 섞인 폴더**: 한 폴더에 DICOM이 있고 그림 파일이 300장(`MIXED_IMAGE_LIMIT`)을 넘으면 그림은 열지 않고 ⚠ 목록에 "이미지 N장을 건너뜀"으로 남깁니다. 그림만 볼 때는 그 폴더만 따로 열면 됩니다.
- 이미지 한 폴더를 한 시리즈로 읽는 상한을 3,000장(`MAX_IMAGE_SEQUENCE`)으로 두어 메모리 폭주를 막습니다.

## 2.13.0 — 2026-09-20

### 추가
- **Help 메뉴에서 바로 의견 보내기**: Help ▸ 🐛 버그 리포트 / 💡 기능 요청 / 📋 질문하기 를 고르면 브라우저에서 GitHub Issue 작성 창이 해당 템플릿으로 열립니다.
- GitHub Issue 템플릿 세 가지(`.github/ISSUE_TEMPLATE/`): 버그 리포트(증상 · 재현 방법 · 기대 동작 · 스크린샷 · 환경), 기능 요청(설명 · 이유 · 대안), 질문(질문 내용 · 관련 기능). 이슈 첫 화면에는 매뉴얼 · 분석 가이드 · 최신 버전 내려받기 링크도 함께 보입니다.

## 2.12.0 — 2026-09-20

### 추가 · 개선
- **ACR 증빙 영상을 DICOM Secondary Capture로 내보내기**: Export ACR Report 창의 "내보낼 것"에서 보고서 / DICOM SC / 둘 다를 고릅니다. 증빙 46장이 표준 SC DICOM(1.2.840.10008.5.1.4.1.1.7)으로 저장되어 PACS 전송이 가능하고, 환자·검사 정보는 팬텀 원본에서 복사해 같은 Study에 붙습니다 (새 시리즈 9001 `ACR QC evidence`, UTF-8 문자셋, BurnedInAnnotation=YES).

## 2.11.0 — 2026-09-20

### 변경
- 이름 바꾸기 키를 운영체제 기본 규칙으로: **macOS Return**(Finder), **Windows · Linux F2**(탐색기). 시리즈 이름은 ⇧Return / Shift+F2. 시리즈 카드 · 시리즈 트리 · ★ Library 목록 · ROI Manager 이름 칸에 모두 적용되고, 우클릭 메뉴의 단축키 표기도 운영체제에 맞게 바뀝니다.
- macOS에서 목록의 '열기'는 더블클릭 또는 ⌘Return (Return은 이름 바꾸기).

## 2.10.1 — 2026-09-20

### 변경
- 일괄 동영상 파일 이름을 `{SeriesDescription}_{순번}` 으로 (001, 002 … 내보내기 순서). 예: `Ax T2_001.mp4`, `SA CINE_002.mp4`. 단일 내보내기는 번호 없이 설명만 (`Ax T2.mp4`). 쓸 수 없는 문자는 `_`로 대체.

## 2.10.0 — 2026-09-20

### 추가 · 개선
- **File ▸ Batch Export Videos…**: 불러온 시리즈를 체크해 한 번에 동영상으로. 전체 선택/해제, 형식(MP4 · AVI · GIF), 재생 시간, 해상도(원본 · 512 · 1024), 현재 W/L 적용 여부, 저장 폴더를 고르고 `3/10 시리즈 변환 중…` 진행 표시. 파일 이름은 `{설명}_{시리즈번호}.{확장자}` (겹치면 _2), 한 시리즈가 실패해도 나머지는 계속.

## 2.9.1 — 2026-09-20

### 버그 수정
- 새 버전 확인이 SSL 인증서 오류로 조용히 실패하던 문제 (파이썬 기본 인증서 저장소가 비어 있는 설치본) → certifi 인증서 사용.

## 2.9.0 — 2026-09-20

### 추가 · 개선
- **새 버전 알림 (앱)**: 시작할 때 GitHub Releases의 최신 태그를 확인해 새 버전이면 알림 (다운로드 페이지 열기 · 나중에 · 이 버전은 다시 알리지 않기). Help ▸ 🔄 새 버전 확인…으로 직접 확인, Settings ▸ 작업 저장에서 끄기. 네트워크가 없으면 조용히 넘어감.
- **새 버전 알림 (웹)**: 배포본과 함께 올라가는 `version.json`과 실행 중인 버전을 비교해 위쪽에 띠 표시. 새로고침 버튼이 캐시·서비스워커를 지우고 최신으로 다시 읽음.

## 2.8.0 — 2026-09-20

### 변경 · 개선 (연동 기능 정리)
- **Crosslink (C)**: 다른 칸 시리즈의 **전체 스캔 범위**를 점선으로, 그 칸의 현재 슬라이스를 노란 실선으로 (예전 Ref Lines의 커버리지 기능을 여기로 옮김).
- **Ref Lines**: 다른 칸의 **현재 슬라이스 한 줄만** 1:1로 표시 (가벼운 기능).
- **Sync Scroll**: 여러 칸 함께 스크롤 (변경 없음).
- **3D Cursor**: 직접 찍은 곳은 빨강, 같은 좌표계의 다른 시리즈는 **초록 + Δ ○○ mm**(슬라이스와 좌표 사이 거리), 스캔 범위 밖이면 커서를 놓지 않고 상태 막대에 "⚠ 대응되는 좌표가 없습니다 (스캔 범위 밖)".
- 3D Cursor 십자선·좌표는 **3D Cursor 도구일 때만** 영상에 표시 (다른 도구에서 좌표가 남아 있던 문제).

## 2.7.0 — 2026-09-20

### 추가 · 개선
- **Multi View 다중 선택 동기화**: Ctrl(⌘)+클릭으로 칸을 하나씩, Shift+클릭으로 범위를 골라 파란 테두리로 표시하고, 고른 칸들이 휠 · ←→(위치) · ↑↓(위상)에 함께 반응합니다.
- **다른 시리즈도 함께 스크롤**: 좌표계가 다르거나 장수가 다르면 장수 비율로 맞춤 (예: SA CINE 270장의 100번째 → 2CH CINE 30장의 11번째). 같은 좌표계·평행이면 예전처럼 위치(mm) 기준.
- **Sync Scroll 버튼**: 켜면 따로 고르지 않아도 보이는 모든 칸이 함께 움직임 (칸을 고르면 선택이 우선).
- Multi View 칸에서도 방향키로 위상·위치 이동 (Phase 띠 없이 시리즈에서 바로 위상 표를 만듦).
- 웹 버전도 같은 동작 (Ctrl·Shift 선택, 비례 동기, 동기 버튼 Y).

## 2.6.2 — 2026-09-20

### 배포
- Release 파일 이름에 버전과 운영체제 표시: `DabbaView-v2.6.2-macOS.zip`, `DabbaView-v2.6.2-Windows.zip`.
- 새 Release가 올라가면 이전 Release는 자동 삭제 (태그는 유지) — Releases 페이지에 최신 하나만 보임.

## 2.6.1 — 2026-09-20

### 버그 수정 · 배포
- Windows 빌드 실패 수정: PyInstaller가 `onnxruntime`을 import하다 러너에서 죽어 빌드가 멈추던 문제 → Windows 앱에서 제외 (ONNX·MedSAM 추론만 빠짐, 나머지는 동일).
- 버전 태그(`v2.6.1` 등)를 올리면 GitHub Actions가 macOS · Windows를 빌드해 **Release**를 만들고 zip을 붙입니다. [Releases](https://github.com/Dabbabbu/DabbaView/releases/latest)에서 로그인 없이 받을 수 있습니다.

## 2.6.0 — 2026-09-20

### 추가 · 개선
- **Tools ▸ 작업 되돌리기 · 원본 복구**: 화면의 ROI · 측정 · 주석, 세그멘테이션 마스크(+ 저장된 npz), 자동 저장 JSON, 원본에 덮어쓴 DICOM(.bak 복구 · 주석 개인 태그 제거)을 골라서 되돌림. 열려 있는 검사의 남은 개수를 보여 줌.
- 종료 창에 세그멘테이션 마스크는 자동 저장된다는 안내 추가.

## 2.5.1 — 2026-09-20

### 변경
- 방향키 방향 바꿈: **← → 슬라이스 위치, ↑ ↓ 위상** (2.5.0과 반대).

## 2.5.0 — 2026-09-20

### 추가 · 개선
- Phase 띠에 **🔒 이 슬라이스에서 위상 보기**: 한 슬라이스 위치에 머문 채 휠 · 시네로 위상을 넘겨 수축기 → 이완기 변화를 한 단면에서 봄.
- **방향키 이동**: ↑ ↓ 슬라이스 위치, ← → 위상 (모드와 관계없이. 위상이 없는 시리즈는 넷 다 슬라이스 이동). Phase 버튼은 포커스를 가져가지 않아 누른 뒤 바로 방향키를 쓸 수 있음.
- 띠 오른쪽에 현재 모드 표시 (`휠 = 위상 (이 슬라이스) · ↑↓ 위치, ←→ 위상`).

## 2.4.1 — 2026-09-20

### 버그 수정
- Phase 번호 띠가 실제 cine 검사에서 보이지 않던 문제: 위치마다 위상 장수가 다르면(일부만 내보낸 검사 — 예: SA CINE 12위치 152장) 위상으로 보지 않고 건너뛰었음. 이제 가운데 값이 5위상 이상이면 띠를 만듦. 위상 태그(TriggerTime · TemporalPositionIdentifier)가 아예 없는 시리즈도 같은 위치의 영상 순서로 위상을 나눔. 시리즈가 다른 경로로 바뀌어도 띠가 따라오도록 보완.

## 2.4.0 — 2026-09-20

### 추가 · 개선
- **종료할 때 작업 저장**: ROI · 측정 · 주석 · Key Image가 있으면 종료 전에 확인 (저장 후 종료 / 저장 없이 종료 / 취소). 저장 방법 세 가지 — 어노테이션만 별도 저장(자동 저장 폴더), 사본 만들어 저장(DICOM 사본 + annotations.json), 원본에 덮어쓰기(개인 태그 (0071,xx01) + .bak).
- **다시 열 때 복원**: 같은 검사를 열면 자동 저장 JSON과 원본 DICOM 개인 태그의 주석을 찾아 복원할지 물어봄.
- **자동 저장**: Settings ▸ 작업 저장에서 간격(기본 5분), 종료할 때 동작(물어보기 · 항상 저장 · 묻지 않음), 다시 열 때 복원 동작 설정.
- Phase 띠에 **[ALL]** 버튼: 전체 위상 스크롤 · 재생 (기본), 번호를 누르면 그 위상만.

### 버그 수정
- 시네 버튼이 다른 툴바 버튼보다 크던 문제 (아이콘 제거, 글자만).
- Library 탭 오른쪽에 밝은 네모가 보이던 문제 (탭 줄 빈 자리 배경).

## 2.3.0 — 2026-09-20

### 추가 · 개선
- **Phase 버튼 띠** (INFINITT PACS 방식): cine · perfusion처럼 한 위치에 여러 위상이 있는 시리즈에서 영상 위에 위상 번호 버튼. 누르면 그 위상으로 (위치 유지), 현재 위상은 노란색, `위치 2/9 · 위상 13/30` 표시. 기본으로 휠은 위치 이동(위상 고정), ▶ Play는 그 위치의 위상 재생 — 체크를 끄면 예전 동작. 위상 구분은 TemporalPositionIdentifier · TriggerTime (5위상 이상).
- 다크 테마 보완: 스크롤 막대 · 표 머리글 · 탭 배경 · 목록을 직접 그리고 팔레트도 어둡게 (macOS에서 Library 탭 등에 밝은 영역이 남던 문제).
- 시네 버튼을 다른 툴바 버튼과 같은 크기로 (글자만, ▶ Play / ■ Stop).

## 2.2.2 — 2026-09-19

### 버그 수정
- 탭 닫기 ✕가 어두운 테마에서 거의 보이지 않던 문제 (밝은 ✕ 버튼으로 바꿈, 새로 생기는 탭 포함).

### 문서
- 매뉴얼 · 분석 가이드를 2.2 기준으로 갱신 (클라우드 경고창, Play/Stop, 방향 문자 · 위상 방향, Ref Lines 스캔 커버리지, 패널 · 탭 닫기와 다시 열기, Settings ▸ Display, ACR 측정 절차 · 증빙 44장 · 뷰어 표시) + 스크린샷 · PDF 새로 만듦.

## 2.2.1 — 2026-09-19

### 버그 수정
- Library 탭을 X로 닫은 뒤 툴바 버튼으로 다시 열리지 않던 문제. 툴바 ★ Library 버튼은 이제 Library 탭 열기/닫기 (보이면 닫고, 닫혔거나 가려져 있거나 왼쪽 패널이 접혀 있으면 열어서 보여 줌). X · 툴바 · View ▸ 패널 메뉴가 같은 동작.
- '현재 스터디를 Library에 추가'는 Ctrl+D와 File 메뉴로 (추가하면 Library 탭이 열림).

## 2.2.0 — 2026-09-19

### 추가 · 개선
- 탭 · 패널 닫기 (X): Series · ★ Library 탭, Image · AI · Analysis · ROI Manager · Histogram/Profile · Python Console 도크. 마우스를 올린 탭(도크 제목)에만 X가 보이고, 누르면 흐려지며 닫힘. 상태바에 다시 여는 방법 표시.
- View ▸ 패널: 닫은 탭 · 패널 다시 열기 (★ Library Ctrl+Shift+L, Image Ctrl+I, AI Ctrl+Shift+A, Python Console F3).
- View ▸ 오버레이 항목: Phase Encoding 방향 · 방향 문자(A/P · R/L · S/I) · 스캔 커버리지 선을 각각 켜고 끔. 기본값은 Settings ▸ Display, T 키는 전체.

## 2.1.0 — 2026-09-19

### 추가 · 개선
- **ACR Phantom QC**
  - 보고서에 측정 과정: 콘솔 수동 절차와 같은 순서 (경사판 ROI 평균 → 기준 레벨 (m1+m2)/4 → W 1 → 경사판 길이 → 두께, 200 cm² ROI → W 1로 가장 어두운·밝은 1 cm² → PIU, 고스팅 배경 ROI → 계산).
  - 증빙 영상 44장: 콘솔 Screen Save 세트와 같은 순서·내용, 검사일·장비·검사자 워터마크. PDF·Word에 포함, JPG로도 저장. 사이트 시퀀스도 자동으로 찾음.
  - 뷰어: 슬라이스마다 쓰이는 검사 배지(값·판정), ROI 적합 초록 / 부적합 빨강, 7개 검사 요약표(클릭하면 해당 영상), 증빙 영상 보기 창.
- **Reference Line**: 다른 시리즈의 전체 스캔 범위를 점선으로(Multi View 칸마다 다른 색), 현재 슬라이스는 노란 실선.
- 영상 가장자리 방향 문자 (L/R · A/P · S/I, 비스듬하면 두 글자)와 위상 인코딩 방향 표시 (0018,1312).
- 시네 버튼: 재생 중 ■ Stop (빨강), 멈춤 ▶ Play (초록).

### 버그 수정
- 클라우드(OneDrive 등) 폴더 경고창의 버튼 · ESC가 눌리지 않고 앱을 끌 수 없던 문제 (진행창이 입력을 막음). 경고창이 떠 있어도 ⌘Q로 종료, 불러오는 중 종료 시 로더를 정리.

## 2.0.0

- 이전 버전 (Library Export, ACR Phantom QC, Image Quality Assessment, 매뉴얼 · 분석 가이드 등).
